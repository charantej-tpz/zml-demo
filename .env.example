# =============================================================================
# Application Environment Configuration
# =============================================================================
# Copy this file to .env and update values for your environment
# For different environments, create: .env.development, .env.staging, .env.production
# =============================================================================

# -----------------------------------------------------------------------------
# ENVIRONMENT (required)
# -----------------------------------------------------------------------------
# Controls which environment the app runs in.
# Options: development | staging | production
# - development: enables debug mode, detailed errors, API docs
# - staging: production-like but for testing
# - production: optimized, docs disabled, JSON logging
ENVIRONMENT=development

# -----------------------------------------------------------------------------
# DEBUG (optional, default: false)
# -----------------------------------------------------------------------------
# Enables debug mode with:
# - Detailed error messages
# - API documentation at /docs and /redoc
# - OpenAPI schema at /openapi.json
# Set to false in staging/production for security
DEBUG=true

# -----------------------------------------------------------------------------
# SERVICE_ACCOUNT_CREDENTIALS_PATH (required for Firebase/GCP)
# -----------------------------------------------------------------------------
# Path to your Firebase Admin SDK / GCP service account JSON file.
# 
# How to get this file:
#   1. Go to Firebase Console → ⚙️ Project Settings → Service accounts
#   2. Click "Generate new private key"
#   3. Download the JSON file and save it securely
#
# This single file provides:
#   - GCP Project ID (extracted automatically)
#   - Authentication for Firestore
#   - Authentication for Realtime Database
#   - Authentication for other Firebase services
#
# Example: /home/user/credentials/firebase-adminsdk.json
SERVICE_ACCOUNT_CREDENTIALS_PATH=

# -----------------------------------------------------------------------------
# REALTIME_DATABASE_URL (required for Realtime Database)
# -----------------------------------------------------------------------------
# Your Firebase Realtime Database URL.
# 
# Find it in: Firebase Console → Realtime Database → Data tab (top of page)
# 
# Format: https://<project-id>.firebaseio.com
# Example: https://my-project-123.firebaseio.com
#
# Leave empty if you're only using Firestore
REALTIME_DATABASE_URL=

# -----------------------------------------------------------------------------
# FIRESTORE_DATABASE_ID (optional, default: "(default)")
# -----------------------------------------------------------------------------
# The Firestore database ID to use.
# - Use "(default)" for the default Firestore database
# - Use a custom ID if you have multiple Firestore databases
FIRESTORE_DATABASE_ID=(default)

# -----------------------------------------------------------------------------
# USE_FIRESTORE_EMULATOR (optional, default: false)
# -----------------------------------------------------------------------------
# Set to true to use local Firestore emulator instead of production.
# Useful for local development without affecting real data.
#
# Start emulator with: firebase emulators:start --only firestore
# Or use docker-compose: docker-compose up firestore-emulator
USE_FIRESTORE_EMULATOR=false
FIRESTORE_EMULATOR_HOST=localhost:9099

# -----------------------------------------------------------------------------
# CORS_ORIGINS (optional, default: http://localhost:3000)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed frontend origins for CORS.
# 
# Examples:
#   - Development: http://localhost:3000,http://localhost:8080
#   - Production: https://myapp.com,https://api.myapp.com
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# -----------------------------------------------------------------------------
# LOG_LEVEL (optional, default: INFO)
# -----------------------------------------------------------------------------
# Controls how much logging output you see.
# Options: DEBUG | INFO | WARNING | ERROR | CRITICAL
#   - DEBUG: Everything (very verbose)
#   - INFO: General operational messages
#   - WARNING: Potential issues
#   - ERROR: Errors that don't crash the app
#   - CRITICAL: Serious errors
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# LOG_JSON_FORMAT (optional, default: false)
# -----------------------------------------------------------------------------
# Set to true for structured JSON logs (recommended for production).
# - false: Human-readable console logs (good for development)
# - true: JSON logs (good for log aggregation tools like Cloud Logging)
LOG_JSON_FORMAT=false

# -----------------------------------------------------------------------------
# APP_NAME / APP_VERSION / API_PREFIX (optional)
# -----------------------------------------------------------------------------
# Application metadata - rarely needs to change
APP_NAME="ZML API"
APP_VERSION="0.1.0"
API_PREFIX="/api/v1"

# -----------------------------------------------------------------------------
# HOST / PORT (optional)
# -----------------------------------------------------------------------------
# Server binding configuration
# Usually you don't need to change these
HOST=0.0.0.0
PORT=8080
